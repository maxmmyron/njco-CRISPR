import React from 'react'

export default function Blob(props) {
    return (
        <div className={`blob ${props.className}`} id={props.id}>
            <svg>
                
            </svg>
        </div>
    )
}
